export const Header = ({ title }) => (
  <header className="flex items-center justify-center h-16 text-gray-800 border-b-2 border-gray-800">
    {/* Use Noto Sans */}
    <span className="">{title}</span>
  </header>
)
