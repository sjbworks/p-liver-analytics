export type TextColorType = 'default' | 'green' | 'primary' | 'secondary'

export type TextSizeType = 'small' | 'medium' | 'large'

export const TextColor = {
  DEFAULT: 'default',
  GREEN: 'green',
  PRIMARY: 'primary',
  SECONDARY: 'secondary',
} as const
